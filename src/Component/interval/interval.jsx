import React, { Component, PropTypes } from 'react';
import './interval.less';

class Main extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return(
      <div id="interval"></div>
    )
  }
}

export default Main;